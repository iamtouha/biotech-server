{
  "routes": [
    {
      "method": "GET",
      "path": "/packs",
      "handler": "pack.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/packs/count",
      "handler": "pack.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/packs/:id",
      "handler": "pack.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/packs",
      "handler": "pack.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/packs/:id",
      "handler": "pack.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/packs/:id",
      "handler": "pack.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
